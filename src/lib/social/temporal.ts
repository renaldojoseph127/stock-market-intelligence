export function moverTemporalRelationship(mentionAt:string,moverDate:string){const mention=new Date(mentionAt),mover=new Date(`${moverDate}T00:00:00Z`);if(Number.isNaN(mention.getTime())||Number.isNaN(mover.getTime()))return{relationshipType:"historical_context",bucket:"unknown",days:null};const mentionDay=Date.UTC(mention.getUTCFullYear(),mention.getUTCMonth(),mention.getUTCDate()),moverDay=mover.getTime(),days=Math.round((moverDay-mentionDay)/86_400_000);if(days===0)return{relationshipType:"mentioned_same_day",bucket:"unknown",days};if(days<0)return{relationshipType:"mentioned_after_move",bucket:"after_move",days};const bucket=days<=1?"within_24h_before":days<=3?"1_to_3_days_before":days<=7?"4_to_7_days_before":days<=14?"8_to_14_days_before":days<=30?"15_to_30_days_before":days<=90?"31_to_90_days_before":"unknown";return{relationshipType:"mentioned_before_move",bucket,days}}
export function catalystTemporalRelationship(postAt:string,eventAt:string){const difference=(new Date(eventAt).getTime()-new Date(postAt).getTime())/86_400_000;if(!Number.isFinite(difference))return{relationshipType:"discussion_near_catalyst",differenceDays:null};if(new Date(postAt).toISOString().slice(0,10)===new Date(eventAt).toISOString().slice(0,10))return{relationshipType:"discussion_same_day",differenceDays:difference};return{relationshipType:difference>0?"discussion_before_catalyst":"discussion_after_catalyst",differenceDays:difference}}
