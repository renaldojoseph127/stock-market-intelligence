import{describe,expect,it}from"vitest";import{normalizeAbsoluteNumber,normalizeCik,normalizeExchange,normalizeSecurityType,normalizeWebsite,validateMetadata}from"../normalize";
describe("ticker metadata normalization",()=>{it("normalizes numeric units into absolute values",()=>{expect(normalizeAbsoluteNumber("$1.25B")).toBe(1_250_000_000);expect(normalizeAbsoluteNumber(12.5,1e6)).toBe(12_500_000);expect(normalizeAbsoluteNumber("-1M")).toBeNull()});it("normalizes exchanges and security types without treating every security as common stock",()=>{expect(normalizeExchange("XNAS")).toBe("NASDAQ");expect(normalizeExchange("XNGS")).toBe("NASDAQ");expect(normalizeExchange("NYSE Amex")).toBe("NYSE American");expect(normalizeExchange("OTCQB")).toBe("OTC");expect(normalizeExchange("OTCM")).toBe("OTC");expect(normalizeSecurityType("Exchange Traded Fund")).toBe("ETF");expect(normalizeSecurityType("Warrant")).toBe("warrant");expect(normalizeSecurityType("Units")).toBe("unit");expect(normalizeSecurityType("American Depositary Shares")).toBe("ADR")});it("accepts canonical company sites and identifiers but rejects aggregators",()=>{expect(normalizeWebsite("example.com")).toBe("https://example.com");expect(normalizeWebsite("https://finance.yahoo.com/quote/XYZ")).toBeNull();expect(normalizeCik(1234)).toBe("0000001234")});it("flags suspicious values rather than silently accepting them",()=>{expect(validateMetadata({market_cap:-1,website:"http://bad url",float_shares:20,shares_outstanding:10,cik:"12"})).toEqual(expect.arrayContaining([expect.stringContaining("market_cap"),expect.stringContaining("website"),expect.stringContaining("CIK"),expect.stringContaining("float_shares")]))})});
