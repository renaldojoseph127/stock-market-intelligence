"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export function PromoterStatusControl({accountId,status,notes}:{accountId:string;status:string;notes?:string|null}){const[msg,setMsg]=useState(""),router=useRouter();async function save(form:FormData){setMsg("Saving…");const r=await fetch(`/api/promoters/${accountId}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))}),b=await r.json();setMsg(b.message);if(r.ok)router.refresh()}return <form action={save} className="panel grid gap-3 p-4 sm:grid-cols-[180px_1fr_auto]"><label className="grid gap-1 text-xs muted">Research status<select name="status" defaultValue={status} className="rounded border border-[#334158] bg-[#0c111b] p-2 text-white">{["unreviewed","candidate","tracked","dismissed"].map(x=><option key={x}>{x}</option>)}</select></label><label className="grid gap-1 text-xs muted">Notes<input name="notes" defaultValue={notes??""} className="rounded border border-[#334158] bg-[#0c111b] p-2 text-white"/></label><button className="self-end rounded bg-blue-600 px-4 py-2 text-sm">Save</button>{msg&&<p className="text-xs text-amber-300 sm:col-span-3">{msg}</p>}</form>}
