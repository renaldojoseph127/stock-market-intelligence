export function DatabaseNotice({configured,error}:{configured:boolean;error:string|null}) { if(configured&&!error)return null; return <div className="mb-5 rounded-lg border border-amber-500/30 bg-amber-500/10 px-4 py-3 text-sm text-amber-200">{!configured?"Database connection is not configured. Add the Supabase environment variables to load live data.":`Database query unavailable: ${error}`}</div>; }
