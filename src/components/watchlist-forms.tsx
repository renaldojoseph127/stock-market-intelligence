"use client";import{useState}from"react";import{useRouter}from"next/navigation";export function CreateWatchlistForm(){const router=useRouter(),[message,setMessage]=useState("");return <form className="panel mb-6 grid gap-3 p-4 sm:grid-cols-[1fr_1fr_160px_auto]" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget),response=await fetch("/api/watchlists",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),description:f.get("description"),watchlistType:f.get("watchlistType")})}),body=await response.json();setMessage(body.message??"Watchlist created.");if(response.ok){e.currentTarget.reset();router.refresh()}}}><label className="grid gap-1 text-xs muted">Name<input required name="name" className="rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"/></label><label className="grid gap-1 text-xs muted">Description<input name="description" className="rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"/></label><label className="grid gap-1 text-xs muted">Type<select name="watchlistType" className="rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"><option>research</option><option>personal</option><option>team</option><option>system</option></select></label><button className="self-end rounded bg-blue-600 px-4 py-2 text-sm">Create</button>{message&&<p className="text-xs muted sm:col-span-4">{message}</p>}</form>}
export function AddWatchlistEntityForm({watchlistId,options}:{watchlistId:string;options:any}){const router=useRouter(),[type,setType]=useState("ticker"),[message,setMessage]=useState(""),rows=type==="ticker"?options.tickers:type==="account"?options.accounts:options.patterns,label=(x:any)=>x.symbol??(x.username?`${x.username} · ${x.platform}`:`${x.name}${x.category_name?` · ${x.category_name}`:""}`);return <form className="panel mb-4 flex flex-wrap items-end gap-3 p-4" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget),response=await fetch(`/api/watchlists/${watchlistId}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"add_entity",entityType:type,entityId:f.get("entityId"),priority:f.get("priority")||null})}),body=await response.json();setMessage(body.message);if(response.ok)router.refresh()}}><label className="grid gap-1 text-xs muted">Entity type<select value={type} onChange={e=>setType(e.target.value)} className="rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"><option value="ticker">Ticker</option><option value="account">Account</option><option value="pattern">Pattern</option></select></label><label className="grid min-w-64 gap-1 text-xs muted">Entity<select required name="entityId" className="rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"><option value="">Select</option>{rows.map((x:any)=><option key={x.id??x.account_id} value={x.id??x.account_id}>{label(x)}</option>)}</select></label>{type==="ticker"&&<label className="grid gap-1 text-xs muted">Priority<input name="priority" type="number" className="w-24 rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"/></label>}<button className="rounded bg-blue-600 px-4 py-2 text-sm">Add</button>{message&&<span className="text-xs muted">{message}</span>}</form>}
export function WatchlistNoteForm({watchlistId}:{watchlistId:string}){const router=useRouter(),[message,setMessage]=useState("");return <form className="panel flex gap-3 p-4" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget),response=await fetch(`/api/watchlists/${watchlistId}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"add_note",note:f.get("note")})}),body=await response.json();setMessage(body.message);if(response.ok){e.currentTarget.reset();router.refresh()}}}><input required name="note" placeholder="Research note" className="min-w-0 flex-1 rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"/><button className="rounded bg-blue-600 px-4 py-2 text-sm">Add note</button>{message&&<span className="self-center text-xs muted">{message}</span>}</form>}
export function WatchlistTagForm({watchlistId,entities}:{watchlistId:string;entities:any[]}){const router=useRouter(),[message,setMessage]=useState("");return <form className="panel mb-4 flex flex-wrap items-end gap-3 p-4" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget),response=await fetch(`/api/watchlists/${watchlistId}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"add_tag",entityId:f.get("entityId"),tagName:f.get("tagName")})}),body=await response.json();setMessage(body.message);if(response.ok){e.currentTarget.reset();router.refresh()}}}><label className="grid gap-1 text-xs muted">Tracked entity<select required name="entityId" className="rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"><option value="">Select</option>{entities.map(x=><option key={x.id} value={x.id}>{x.symbol??x.username??x.pattern_name}</option>)}</select></label><label className="grid gap-1 text-xs muted">Research tag<input required maxLength={80} name="tagName" placeholder="e.g. follow-up" className="rounded border border-[#334158] bg-[#0c111b] px-3 py-2 text-sm text-white"/></label><button className="rounded bg-blue-600 px-4 py-2 text-sm">Apply tag</button>{message&&<span className="text-xs muted">{message}</span>}</form>}
export function RemoveWatchlistEntityButton({watchlistId,entityId}:{watchlistId:string;entityId:string}){const router=useRouter();return <button className="text-xs text-red-300" onClick={async()=>{await fetch(`/api/watchlists/${watchlistId}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"remove_entity",entityId})});router.refresh()}}>Remove</button>}
export function RemoveWatchlistTagButton({watchlistId,entityId,tagId,name}:{watchlistId:string;entityId:string;tagId:string;name:string}){const router=useRouter();return <button className="rounded bg-[#182235] px-2 py-1 text-xs" title={`Remove ${name}`} onClick={async()=>{await fetch(`/api/watchlists/${watchlistId}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"remove_tag",entityId,tagId})});router.refresh()}}>{name} ×</button>}
