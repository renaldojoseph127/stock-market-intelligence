import Link from"next/link";import{PatternObservationsTable}from"@/components/pattern-components";import{getTickerPatterns}from"@/lib/patterns/queries";export async function TickerPatternHistory({tickerId,symbol}:{tickerId:string;symbol:string}){const{data}=await getTickerPatterns(tickerId);return <section className="mt-8"><div className="mb-3 flex items-center justify-between"><h2 className="font-semibold">Historical Patterns</h2><Link className="text-sm text-blue-400" href={`/research/patterns?similarTicker=${encodeURIComponent(symbol)}`}>Find similar situations</Link></div><PatternObservationsTable rows={data}/><p className="mt-3 text-xs muted">These are historical condition matches. They do not imply causality or future results.</p></section>}
