import Link from"next/link";import{DataTable,EmptyState,TableCell}from"./ui";
export function PromoterLeaderboardTable({rows}:{rows:any[]}){if(!rows.length)return <EmptyState title="No account intelligence" description="No real social accounts match this filter. No sample promoters are created."/>;return <DataTable headers={["Username","Platform","Tickers","Mentions","Early Mentions","Gainer","Decliner","Most Active","Avg Days Before","First Active","Last Active","Status"]}>{rows.map(x=><tr key={x.account_id}><TableCell><Link className="font-medium text-blue-400" href={`/promoters/${x.account_id}`}>{x.username}</Link></TableCell><TableCell>{x.platform}</TableCell><TableCell>{x.unique_tickers}</TableCell><TableCell>{x.total_ticker_mentions}</TableCell><TableCell>{x.early_mentions}</TableCell><TableCell>{x.gainer_associations}</TableCell><TableCell>{x.decliner_associations}</TableCell><TableCell>{x.most_active_associations}</TableCell><TableCell>{x.average_days_before_mover??"—"}</TableCell><TableCell>{x.first_activity??"—"}</TableCell><TableCell>{x.last_activity??"—"}</TableCell><TableCell>{x.promoter_status}</TableCell></tr>)}</DataTable>}
