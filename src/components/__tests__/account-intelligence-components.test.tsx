import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{PromoterLeaderboardTable}from"../account-intelligence-components";
describe("promoter table",()=>{it("renders without data",()=>expect(renderToStaticMarkup(<PromoterLeaderboardTable rows={[]}/>)).toContain("No account intelligence"));it("renders real-shaped account data",()=>{const html=renderToStaticMarkup(<PromoterLeaderboardTable rows={[{account_id:"a1",username:"researcher",platform:"Reddit",unique_tickers:2,total_ticker_mentions:7,early_mentions:3,gainer_associations:2,decliner_associations:1,most_active_associations:1,average_days_before_mover:4,first_activity:"2025-01-01",last_activity:"2025-02-01",promoter_status:"candidate"}]}/>);expect(html).toContain("researcher");expect(html).toContain("candidate");expect(html).toContain("Reddit")})});
