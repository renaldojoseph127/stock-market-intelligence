import{NextResponse}from"next/server";import{createAdminClient}from"@/lib/supabase/admin";export const runtime="nodejs";export async function POST(){const db=createAdminClient();if(!db)return NextResponse.json({message:"Dedicated Supabase service credentials are not configured."},{status:503});const{data,error}=await(db as any).rpc("rebuild_research_search_documents");return NextResponse.json(error?{message:error.message}:{result:data},{status:error?500:200})}
