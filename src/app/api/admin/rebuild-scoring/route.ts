import{NextResponse}from"next/server";import{createAdminClient}from"@/lib/supabase/admin";export const runtime="nodejs";
export async function POST(request:Request){const db=createAdminClient();if(!db)return NextResponse.json({message:"Dedicated Supabase service credentials are not configured."},{status:503});let input:any={};try{input=await request.json()}catch{}const{data,error}=await(db as any).rpc("rebuild_cp6_analytics",{p_ticker_id:input.tickerId||null,p_source_id:input.sourceId||null,p_start_at:input.startAt||null,p_end_at:input.endAt||null});return NextResponse.json(error?{message:error.message}:{result:data},{status:error?500:200})}
