import{NextResponse}from"next/server";import{createAdminClient}from"@/lib/supabase/admin";export const runtime="nodejs";export async function POST(request:Request){const db=createAdminClient();if(!db)return NextResponse.json({message:"Dedicated Supabase service credentials are not configured."},{status:503});let body:any={};try{body=await request.json()}catch{}const{data,error}=await(db as any).rpc("rebuild_cp8_patterns",{p_ticker_id:body.tickerId||null,p_start_date:body.startDate||null,p_end_date:body.endDate||null});return NextResponse.json(error?{message:error.message}:{result:data},{status:error?500:200})}
