import{DatabaseNotice}from"@/components/database-notice";import{Pagination,SocialPostsTable}from"@/components/social-components";import{DateRangeFilter,Field,FilterBar,PageHeader}from"@/components/ui";import{getSocialPosts}from"@/lib/social/queries";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const s=await searchParams,r=await getSocialPosts(s);return <><PageHeader title="Social Search" description="Server-side search across normalized posts, accounts, communities, sources, dates, and resolved tickers."/><DatabaseNotice configured={r.configured} error={r.error}/><form><FilterBar><DateRangeFilter from={s.from} to={s.to}/><Field label="Source" name="source" defaultValue={s.source}/><Field label="Ticker" name="ticker" defaultValue={s.ticker}/><Field label="Username" name="username" defaultValue={s.username}/><Field label="Community" name="community" defaultValue={s.community}/><Field label="Keyword" name="keyword" defaultValue={s.keyword}/><button className="rounded-md bg-blue-600 px-4 py-2 text-sm">Search</button></FilterBar></form><SocialPostsTable posts={r.data}/><Pagination page={Number(s.page)||1} params={s}/></>}
