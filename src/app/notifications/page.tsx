import{AlertEventsTable}from"@/components/alert-components";import{AlertEventActions}from"@/components/alert-forms";import{Pagination}from"@/components/analytics-components";import{DatabaseNotice}from"@/components/database-notice";import{Field,FilterBar,PageHeader}from"@/components/ui";import{getNotifications}from"@/lib/alerts/queries";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams,r=await getNotifications(p);return <><PageHeader title="In-App Notification Center" description="Recent evidence-backed research alerts and review status."/><DatabaseNotice configured={r.configured} error={r.error}/><form><FilterBar><Field label="Ticker" name="ticker" defaultValue={p.ticker}/><Field label="Account" name="account" defaultValue={p.account}/><Field label="Pattern" name="pattern" defaultValue={p.pattern}/><Field label="Alert type" name="condition" defaultValue={p.condition}/><Field label="Severity" name="severity" defaultValue={p.severity} options={["low","medium","high","critical"]}/><Field label="Status" name="status" defaultValue={p.status} options={["new","reviewed","dismissed","archived"]}/><Field label="From" name="from" type="date" defaultValue={p.from}/><Field label="To" name="to" type="date" defaultValue={p.to}/><button className="rounded bg-blue-600 px-4 py-2 text-sm">Apply</button></FilterBar></form><AlertEventsTable rows={r.data} actions={e=><AlertEventActions event={e}/>}/><Pagination path="/notifications" p={p} page={r.page} hasNext={r.data.length===r.pageSize}/></>}
