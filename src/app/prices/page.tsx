import{Pagination}from"@/components/analytics-components";import{DatabaseNotice}from"@/components/database-notice";import{PriceRecordsTable}from"@/components/price-components";import{Field,FilterBar,PageHeader}from"@/components/ui";import{getPrices}from"@/lib/prices/queries";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams,r=await getPrices(p);return <><PageHeader title="Historical Prices" description="Normalized historical OHLCV records and derived, backward-looking market metrics."/><DatabaseNotice configured={r.configured} error={r.error}/><form><FilterBar><Field label="Ticker" name="ticker" defaultValue={p.ticker}/><Field label="From" name="from" type="date" defaultValue={p.from}/><Field label="To" name="to" type="date" defaultValue={p.to}/><Field label="Source" name="source" defaultValue={p.source}/><button className="rounded bg-blue-600 px-4 py-2 text-sm">Apply</button></FilterBar></form><PriceRecordsTable rows={r.data}/><Pagination path="/prices" p={p} page={r.page} hasNext={r.data.length===r.pageSize}/></>}
