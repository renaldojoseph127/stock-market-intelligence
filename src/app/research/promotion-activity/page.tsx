import Link from"next/link";import{Pagination}from"@/components/analytics-components";import{DatabaseNotice}from"@/components/database-notice";import{DataTable,DateRangeFilter,EmptyState,Field,FilterBar,PageHeader,TableCell,TickerLink}from"@/components/ui";import{getPromotionActivity}from"@/lib/scoring/queries";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams,r=await getPromotionActivity(p);return <><PageHeader title="Promotion Activity" description="Observable discussion bursts and promotional-style activity. This does not classify illegal promotion."/><DatabaseNotice configured={r.configured} error={r.error}/><form><FilterBar><DateRangeFilter from={p.from} to={p.to}/><Field label="Ticker" name="ticker" defaultValue={p.ticker}/><Field label="Platform" name="platform" defaultValue={p.platform}/><Field label="Min intensity" name="minIntensity" type="number" defaultValue={p.minIntensity}/><Field label="Min hype risk" name="minHype" type="number" defaultValue={p.minHype}/><Field label="Sort" name="sort" defaultValue={p.sort} options={["event_start_at","promotion_intensity","unusual_attention_score","hype_risk_score"]}/><Field label="Direction" name="direction" defaultValue={p.direction} options={["desc","asc"]}/><button className="rounded bg-blue-600 px-4 py-2 text-sm">Apply</button></FilterBar></form>{r.data.length?<DataTable headers={["Ticker","Start","End","Platform","Mentions","Accounts","Intensity","Attention","Hype Risk","Explain"]}>{r.data.map(x=><tr key={x.id}><TableCell><TickerLink symbol={x.symbol}/></TableCell><TableCell>{x.event_start_at}</TableCell><TableCell>{x.event_end_at}</TableCell><TableCell>{x.platform}</TableCell><TableCell>{x.mention_count}</TableCell><TableCell>{x.unique_accounts}</TableCell><TableCell>{x.promotion_intensity}</TableCell><TableCell>{x.unusual_attention_score}</TableCell><TableCell>{x.hype_risk_score}</TableCell><TableCell><Link className="text-blue-400" href={`/research/promotion-activity/${x.id}`}>Why these scores?</Link></TableCell></tr>)}</DataTable>:<EmptyState title="No promotion activity events" description="No deterministic discussion bursts match this filter."/>}<Pagination path="/research/promotion-activity" p={p} page={r.page} hasNext={r.data.length===r.pageSize}/><p className="mt-4 text-xs muted">Hype Risk is a research metric describing unusual social-market attention patterns. It is not evidence of fraud, manipulation, or future price performance.</p></>}
