import{Pagination}from"@/components/analytics-components";import{DatabaseNotice}from"@/components/database-notice";import{SocialOutcomesTable}from"@/components/price-components";import{Field,FilterBar,PageHeader}from"@/components/ui";import{getSocialOutcomes}from"@/lib/prices/queries";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams,r=await getSocialOutcomes(p);return <><PageHeader title="Social Market Outcomes" description="Historical market observations after imported social mentions. Sequence does not establish causality."/><DatabaseNotice configured={r.configured} error={r.error}/><form><FilterBar><Field label="Ticker" name="ticker" defaultValue={p.ticker}/><Field label="Account" name="account" defaultValue={p.account}/><Field label="Source" name="source" defaultValue={p.source}/><Field label="Sentiment" name="sentiment" defaultValue={p.sentiment} options={["very_bullish","bullish","neutral","bearish","very_bearish"]}/><Field label="Min attention" name="minAttention" type="number" defaultValue={p.minAttention}/><Field label="Max attention" name="maxAttention" type="number" defaultValue={p.maxAttention}/><Field label="Min 7D return %" name="minReturn" type="number" defaultValue={p.minReturn}/><Field label="Max 7D return %" name="maxReturn" type="number" defaultValue={p.maxReturn}/><button className="rounded bg-blue-600 px-4 py-2 text-sm">Apply</button></FilterBar></form><SocialOutcomesTable rows={r.data}/><Pagination path="/research/social-outcomes" p={p} page={r.page} hasNext={r.data.length===r.pageSize}/><p className="mt-3 text-xs muted">Returns use raw close and the next available recorded sessions. They are historical observations, not predictions or recommendations.</p></>}
