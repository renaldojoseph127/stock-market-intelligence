import{DatabaseNotice}from"@/components/database-notice";import{DataTable,EmptyState,PageHeader,TableCell}from"@/components/ui";import{getAttentionComponents}from"@/lib/scoring/queries";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,r=await getAttentionComponents(id);return <><PageHeader title="Attention Score Breakdown" description="Auditable normalized components and weighted contributions."/><DatabaseNotice configured={r.configured} error={r.error}/>{r.data.length?<DataTable headers={["Component","Raw Value","Normalized","Weight","Contribution","Explanation"]}>{r.data.map(x=><tr key={x.id}><TableCell>{x.component_name}</TableCell><TableCell>{x.raw_value??"Unavailable"}</TableCell><TableCell>{x.normalized_value??"Excluded"}</TableCell><TableCell>{x.weight}</TableCell><TableCell>{x.contribution??"Excluded"}</TableCell><TableCell className="max-w-lg whitespace-normal">{x.explanation}</TableCell></tr>)}</DataTable>:<EmptyState title="No component data" description="The attention observation does not exist or has not been rebuilt."/>}</>}
