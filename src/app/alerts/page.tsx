import Link from"next/link";import{AlertEventsTable,AlertRulesTable}from"@/components/alert-components";import{AlertRuleActions,EvaluateAlertsButton}from"@/components/alert-forms";import{Pagination}from"@/components/analytics-components";import{DatabaseNotice}from"@/components/database-notice";import{Field,FilterBar,PageHeader}from"@/components/ui";import{getAlertRules,getNotifications}from"@/lib/alerts/queries";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams,[rules,events]=await Promise.all([getAlertRules(p),getNotifications({...p,page:undefined})]);return <><PageHeader title="Alert Management" description="Structured evidence-backed research condition monitoring." action={<div className="flex gap-3"><EvaluateAlertsButton/><Link className="rounded border border-[#334158] px-4 py-2 text-sm" href="/alerts/new">New alert</Link></div>}/><DatabaseNotice configured={rules.configured} error={rules.error||events.error}/><form><FilterBar><Field label="Status" name="status" defaultValue={p.status} options={["enabled","disabled"]}/><Field label="Entity" name="entity" defaultValue={p.entity} options={["ticker","account","pattern"]}/><Field label="Condition" name="condition" defaultValue={p.condition}/><button className="rounded bg-blue-600 px-4 py-2 text-sm">Apply</button></FilterBar></form><AlertRulesTable rows={rules.data} actions={x=><AlertRuleActions rule={x}/>}/><Pagination path="/alerts" p={p} page={rules.page} hasNext={rules.data.length===rules.pageSize}/><section className="mt-8"><h2 className="mb-3 font-semibold">Recent Trigger History</h2><AlertEventsTable rows={events.data}/></section></>}
