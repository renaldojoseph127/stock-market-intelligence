import{DatabaseNotice}from"@/components/database-notice";import{PatternLibraryTable}from"@/components/pattern-components";import{Field,FilterBar,PageHeader}from"@/components/ui";import{getPatternLibrary}from"@/lib/patterns/queries";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams,r=await getPatternLibrary(p);return <><PageHeader title="Historical Pattern Research" description="Transparent recurring historical situations, ordered by frequency rather than predictive ranking."/><DatabaseNotice configured={r.configured} error={r.error}/><form><FilterBar><Field label="Category" name="category" defaultValue={p.category} options={["social","market","combined"]}/><Field label="Status" name="active" defaultValue={p.active} options={["true","false"]}/><Field label="Sort" name="sort" defaultValue={p.sort} options={["total_occurrences","last_seen","name"]}/><Field label="Direction" name="direction" defaultValue={p.direction} options={["desc","asc"]}/><button className="rounded bg-blue-600 px-4 py-2 text-sm">Apply</button></FilterBar></form><PatternLibraryTable rows={r.data}/><p className="mt-3 text-xs muted">Historical similarity and frequency do not imply future results. Patterns are descriptive observations, not predictions, ratings, or recommendations.</p></>}
